<%# div_for comment, :class  => "foo" do %>
<div id="comment">
	<p>
		Posted at <strong><%= time_ago_in_words(comment.created_at) %></strong> ago<br />
		<%= h(comment.body)%>
		<br />
	</p>	
</div>
<%# end %>